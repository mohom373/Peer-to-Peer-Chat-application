<UserControl x:Class="P2PChatProj.Views.UserOnlineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:P2PChatProj.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    
    <Grid Background="AliceBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <local:MenuView  Grid.Column="0" />
        <local:ChatView Grid.Column="1" />

        <!--<ContentControl x:Name="MenuView" Content="{Binding Path=Menu}" Grid.Column="0" />
        <ContentControl x:Name="ChatView" Content="{Binding Path=Chat}" Grid.Column="1" />-->
        
    </Grid>
</UserControl>
