<UserControl x:Class="P2PChatProj.Views.OfflineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:P2PChatProj.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">


    <Grid Background="#1C7293">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="1">

            <TextBlock FontSize="36" FontWeight="Bold" Text="P2P Chat" HorizontalAlignment="Center"/>

            <TextBlock Text="Username" Margin="0 10 0 5" />
            <TextBox x:Name="UserNameInput" Width="220" Padding="5"
                 Text="{Binding Path= User.UserName, Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

            <TextBlock Text="Port Number" Margin="0 10 0 5" />
            <TextBox x:Name="PortNumberInput" Width="220" Padding="5"
                 Text="{Binding Path= User.PortNumber, Mode=TwoWay, 
                        UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <Button x:Name="submitButton" Content="Go Online" FontWeight="DemiBold" 
                    Margin="0,20,0,0" Padding="10" HorizontalAlignment="Center" 
                    Command="{Binding GoOnlineButtonCommand}"/>

        </StackPanel>

    </Grid>
</UserControl>
